services:
  db:
    image: postgres:15
    container_name: compass-postgres-container
    env_file:
      - .env
    ports:
      - "5433:5432"
    # adding volume for seeding database
    volumes:
      - ./server/seed_db:/seed_db
  redis:
    image: redis:latest
    container_name: compass-redis-container
    ports:
      - "6379:6379"

  backend:
    build: ./server
    container_name: compass-django-container
    command: sh -c "gunicorn compass_api.wsgi --bind 0.0.0.0:8000 --reload"
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - ./server:/app
    depends_on:
      - db
      - redis

  frontend:
    image: client-img
    build: ./client
    container_name: compass-client-container
    ports:
      - "80:80"
    volumes:
        - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
        - ./client/dist:/usr/share/nginx/html
    depends_on:
      - backend
